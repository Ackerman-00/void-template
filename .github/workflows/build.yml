name: Build Custom XBPS
on:
  workflow_dispatch:
    inputs:
      package_name:
        description: 'Which package to build?'
        required: true
        default: 'niri'
        type: choice
        options:
          - niri
          - vesktop
          - zen-browser
          - gpu-screen-recorder

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-glibc-full:20250616R1
      options: --privileged
      volumes:
        - /dev:/dev

    steps:
      - name: Prepare container
        run: |
          mkdir -p /etc/xbps.d && cp /usr/share/xbps.d/*-repository-*.conf /etc/xbps.d/
          sed -i 's|repo-default|repo-ci|g' /etc/xbps.d/*-repository-*.conf
          xbps-install -Syu xbps && xbps-install -yu && xbps-install -y sudo bash git
          useradd -G xbuilder -M builder

      - name: Checkout your template
        uses: actions/checkout@v4
        with:
          path: custom-repo

      - name: Setup void-packages
        run: |
          git clone --depth=1 https://github.com/void-linux/void-packages.git void-packages
          cp -r custom-repo/${{ inputs.package_name }} void-packages/srcpkgs/
          
          # Configure for Ethereal mode (to avoid uid_map errors)
          echo 'XBPS_CHROOT_CMD=ethereal' >> void-packages/etc/conf
          echo 'XBPS_ALLOW_CHROOT_BREAKOUT=yes' >> void-packages/etc/conf
          
          # Initialize the masterdir for ethereal mode
          cd void-packages
          mkdir -p masterdir
          
          # Give builder ownership of everything
          cd ..
          chown -R builder:builder .

      - name: Build package
        run: |
          cd void-packages
          # The key fix: bootstrap-ethereal prepares the masterdir without a full install
          sudo -Eu builder ./xbps-src bootstrap-ethereal
          # Now it will find the 'bootstrap' and proceed to build
          sudo -Eu builder ./xbps-src pkg ${{ inputs.package_name }}

      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.package_name }}-binary
          path: "void-packages/hostdir/binpkgs/**/*.xbps"
